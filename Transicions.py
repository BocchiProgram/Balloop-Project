import Balloop
cores = ['purple', 'blue'], ['black', 'red',], ['yellow', 'purple'], ['green', 'black']
class Trans:
    # def __init__(self, x):
    #     for c in range()
    #     
    def backgroud_color(x):
        return cores[x - 1][0]

    def player_color(x):
        return cores[x - 1][1]
